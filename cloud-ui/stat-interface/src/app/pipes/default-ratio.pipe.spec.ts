import { DefaultRatioPipe } from './default-ratio.pipe';

describe('DefaultRatioPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultRatioPipe();
    expect(pipe).toBeTruthy();
  });
});
